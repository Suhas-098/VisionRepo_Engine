version: '3.9'

services:
  backend:
    build: ./Server
    ports:
      - "3000:3000"
    env_file:
      - ./Server/.env

  frontend:
    build: ./client
    ports:
      - "5173:5173"
    volumes:
      - ./client:/app
      - /app/node_modules
    depends_on:
      - backend
